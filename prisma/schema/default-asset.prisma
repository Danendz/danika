model DefaultAsset {
  id         String   @id @default(uuid(7))
  key        String   @unique
  url        String
  created_at DateTime @default(now())
  update_at  DateTime @updatedAt

  @@map("default_assets")
}
